vars:
  - name: TEAM
    value: platform
labels:
  team: "{{var:TEAM}}"
container_envs:
  - name: "*"
    envs:
      - name: GLOBAL_FLAG
        value: "true"
      - name: REMOVE_ME
        value: "from-defaults"
  - name: api
    envs:
      - name: API_DEFAULT
        value: "from-container-defaults"
