vars:
  - name: TEAM
    value: platform
labels:
  team: "{{var:TEAM}}"
container_vars:
  - name: "*"
    vars:
      - name: GLOBAL_FLAG
        value: "true"
      - name: REMOVE_ME
        value: "from-defaults"
  - name: api
    vars:
      - name: API_DEFAULT
        value: "from-container-defaults"
